pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo 'Hello World'
            }
        }
		stage('build') {
		    steps {
			 echo 'Hi'
			}
		}
        stage('Deploy') {
		    steps {
			 echo 'Hi Hello'
			}
        }			
    }
}
